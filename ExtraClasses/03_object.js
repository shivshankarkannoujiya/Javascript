// Function are object
// Iterating throughh Object

// For of
// for in

const rectangle = {
  length: 12,
  breadht: 10,
};

// for in loop
for (let key in rectangle) {
  // console.log(key);
  console.log(key, rectangle[key]);
}
